<div class="container">
    <%# Links %>
    <%= render "questions/links" %>


    <table class="table table-dark table-striped">
        <thead>
            <tr>
                <%# Question ID Heading %>
                <th scope="col">Id</th>

                <%# Question main Heading %>
                <th scope="col">Question</th>

                <%# Answer Heading %>
                <th scope="col">Answer</th>

                <%# Keyword Heading %>
                <th scope="col">Keyword</th>

                <%# Amount of attempts Heading %>
                <th scope="col">Attempts</th>

                <%# Amount of correct answers Heading %>
                <th scope="col">Correct</th>

                <%# Amount of comments Heading %>
                <th scope="col">Comments</th>
            </tr>
        </thead>

        <tbody>
            <% @questions.each do |question| %>
                <tr>
                    <%# Question ID %>
                    <td><%= link_to question.id, question_path(question) %></td>

                    <%# Question main %>
                    <td><%= question.main %></td>

                    <%# Answer %>
                    <td><%= question.answer %></td>

                    <%# Keyword %>
                    <td><%= question.keyword %></td>

                    <%# Amount of attempts %>
                    <td><%= question.attempted %></td>

                    <%# Amount of correct answer %>
                    <td><%= question.correct %></td>

                    <%# Amount of comments %>
                    <td><%= pluralize(question.comments.size, "Comment") %></td>
                </tr>
            <% end %>
        </tbody>
    </table>
</div>
